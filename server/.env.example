PORT=5000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key
# Optional: connect to MongoDB instance. If left blank, server defaults to in-memory MongoDB for local execution.
# MONGODB_URI=mongodb://localhost:27017/jayam_vpms
